/**
 * Data Seeding Disabled
 * All patients, appointments, billing, staff, and doctors are created manually by the clinic staff.
 */
export const seedDatabase = async () => {
  // Seeding completely disabled to keep the production database clean.
  return;
};
